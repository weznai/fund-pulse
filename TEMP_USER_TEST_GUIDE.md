# 临时客户改造测试指南

## 🧪 快速验证（5分钟）

### 测试1：临时客户累计收益显示

```
步骤：
1. 打开浏览器（Chrome/Edge）
2. 打开开发者工具（F12）-> Application -> Local Storage
3. 清除所有本地数据（Clear storage）
4. 访问网站首页
5. 添加一个基金持仓（例如：000001）
6. 查看收益分析页面

预期结果：
✅ 累计收益卡片显示金额（不是0）
✅ 累计收益卡片右上角有"临时"标记
✅ 页面顶部有黄色提示框："您当前是访客模式"
✅ 控制台日志显示："📦 使用 localStorage 模式"

如果失败，检查控制台错误信息。
```

### 测试2：登录用户功能不受影响

```
步骤：
1. 登录系统
2. 查看基金列表
3. 查看收益分析页面

预期结果：
✅ 累计收益显示后端结算值
✅ 累计收益卡片无"临时"标记
✅ 收益日历正常显示
✅ 分时数据正常显示
✅ 页面顶部无临时用户提示

如果失败，说明登录用户的代码被意外修改了，需要回滚。
```

### 测试3：数据迁移兼容性

```
步骤：
1. 创建旧版本测试数据（在控制台执行）：
   localStorage.setItem('fund_holdings', JSON.stringify({
     '000001': {
       fundCode: '000001',
       fundName: '华夏成长混合',
       share: 1000,
       cost: 1.5,
       amount: 1500,
       holdingDate: '2025-01-01'
     }
   }))

2. 刷新页面
3. 查看控制台日志
4. 查看 localStorage 中的数据

预期结果：
✅ 控制台日志显示："🔄 检测到旧版本数据，开始迁移..."
✅ 控制台日志显示："✅ 数据迁移完成: 升级 1 条持仓，0 个错误"
✅ localStorage 中有 'fund_holdings_version' = 'v2'
✅ 数据中新增了 calculatedTotalProfit 等字段
```

### 测试4：切换用户状态

```
步骤：
1. 临时客户状态
   - 查看累计收益（前端计算）
   - 查看是否有"临时"标记

2. 登录
   - 查看累计收益（后端结算）
   - 查看是否无"临时"标记

3. 退出登录
   - 查看累计收益（恢复前端计算）
   - 查看是否有"临时"标记

预期结果：
✅ 临时客户：显示"临时"标记，累计收益为前端计算
✅ 登录后：无"临时"标记，累计收益为后端结算
✅ 退出后：恢复"临时"标记，累计收益为前端计算
```

---

## 🔍 详细测试（30分钟）

### 1. 边界情况测试

#### 测试1.1：无持仓的临时客户
```
步骤：
1. 清除所有数据
2. 访问网站（无持仓）
3. 查看收益分析页面

预期结果：
✅ 累计收益显示 ¥0.00
✅ 无错误提示
✅ 临时用户提示正常显示
```

#### 测试1.2：多个持仓的临时客户
```
步骤：
1. 添加5个不同基金
2. 设置不同的成本价和份额
3. 查看累计收益
4. 手动计算验证

预期结果：
✅ 累计收益 = Σ((当前净值 - 成本价) * 份额)
✅ 每个基金的当日收益计算正确
✅ 总累计收益计算正确
```

#### 测试1.3：份额为0的持仓
```
步骤：
1. 添加一个持仓
2. 设置份额为0
3. 查看累计收益

预期结果：
✅ 累计收益显示 0
✅ 不报错
```

#### 测试1.4：成本价为0的持仓
```
步骤：
1. 添加一个持仓
2. 设置成本价为0
3. 查看累计收益

预期结果：
✅ 累计收益显示 0 或不显示
✅ 不报错
```

### 2. 数据完整性测试

#### 测试2.1：数据持久化
```
步骤：
1. 临时客户添加持仓
2. 刷新页面
3. 查看数据是否丢失

预期结果：
✅ 持仓数据不丢失
✅ 累计收益数据不丢失
✅ 计算的字段正确保存
```

#### 测试2.2：数据格式验证
```
步骤：
1. 添加持仓
2. 在控制台查看 localStorage 数据：
   JSON.parse(localStorage.getItem('fund_holdings'))

预期结果：
✅ 数据包含所有必要字段
✅ 数据类型正确（number, string等）
✅ 有 calculatedTotalProfit 等新增字段
✅ 有 lastCalculatedDate 字段
```

### 3. 兼容性测试

#### 测试3.1：旧版本数据升级
```
步骤：
1. 手动创建旧版本数据（无新增字段）
2. 刷新页面
3. 查看数据是否升级

预期结果：
✅ 数据自动升级
✅ 保留原有字段
✅ 新增字段有默认值
✅ 版本标记更新
```

#### 测试3.2：跨浏览器测试
```
步骤：
1. 在 Chrome 测试
2. 在 Edge 测试
3. 在 Firefox 测试（如果支持）

预期结果：
✅ 所有浏览器功能正常
✅ 数据格式一致
✅ 显示效果一致
```

#### 测试3.3：移动端测试
```
步骤：
1. 使用 Chrome DevTools 切换到移动端视图
2. 或在真实手机上测试

预期结果：
✅ 临时用户提示显示正常（响应式）
✅ 累计收益显示正常
✅ "临时"标记显示正常
```

### 4. 性能测试

#### 测试4.1：大量持仓性能
```
步骤：
1. 添加50个基金持仓
2. 刷新页面
3. 观察加载时间

预期结果：
✅ 页面加载时间 < 2秒
✅ 累计收益计算时间 < 100ms
✅ 无卡顿
```

#### 测试4.2：计算准确性
```
步骤：
1. 添加一个持仓：份额1000，成本1.5，净值2.0
2. 手动计算：累计收益 = (2.0 - 1.5) * 1000 = 500
3. 对比系统显示

预期结果：
✅ 系统显示 ¥500.00
✅ 计算误差 < 0.01
```

---

## 🐛 常见问题排查

### 问题1：累计收益仍然显示0

**可能原因：**
- 用户已登录（系统使用后端结算数据）
- 持仓数据有问题
- 净值数据未加载

**排查步骤：**
```javascript
// 1. 检查用户状态
console.log('useDatabase:', fundStore.useDatabase)

// 2. 检查持仓数据
console.log('holdings:', fundStore.holdings.holdings)

// 3. 检查基金数据
console.log('funds:', fundStore.sortedFavorites)

// 4. 检查净值数据
console.log('nav:', fundStore.sortedFavorites[0]?.nav)
```

### 问题2：数据迁移失败

**可能原因：**
- localStorage 数据损坏
- 数据格式异常

**排查步骤：**
```javascript
// 1. 检查原始数据
const raw = localStorage.getItem('fund_holdings')
console.log('原始数据:', raw)

// 2. 尝试解析
try {
  const parsed = JSON.parse(raw)
  console.log('解析成功:', parsed)
} catch (error) {
  console.error('解析失败:', error)
}

// 3. 手动清除并重试
localStorage.clear()
location.reload()
```

### 问题3：登录用户看到"临时"标记

**可能原因：**
- 用户状态判断错误
- useDatabase 状态未正确更新

**排查步骤：**
```javascript
// 检查用户状态
console.log('isLoggedIn:', isLoggedIn.value)
console.log('useDatabase:', fundStore.useDatabase)
```

**临时解决方案：**
```javascript
// 强制刷新状态
fundStore.loadFromDatabase()
```

---

## ✅ 测试清单

### 临时客户测试

- [ ] 累计收益正常显示（不为0）
- [ ] 累计收益卡片有"临时"标记
- [ ] 页面顶部有临时用户提示
- [ ] 当日收益计算正确
- [ ] 数据持久化正常
- [ ] 刷新页面数据不丢失
- [ ] 切换到登录用户功能正常

### 登录用户测试

- [ ] 累计收益正常显示（后端结算）
- [ ] 累计收益卡片无"临时"标记
- [ ] 页面顶部无临时用户提示
- [ ] 收益历史正常显示
- [ ] 收益日历正常显示
- [ ] 分时数据正常显示
- [ ] 所有功能与改造前一致

### 数据迁移测试

- [ ] 旧版本数据自动升级
- [ ] 升级过程无错误
- [ ] 升级后数据完整
- [ ] 版本标记正确更新
- [ ] 升级失败时保留旧数据

### 边界情况测试

- [ ] 无持仓时不报错
- [ ] 份额为0时不报错
- [ ] 成本价为0时不报错
- [ ] 净值为0时不报错
- [ ] 大量持仓性能正常
- [ ] 移动端显示正常

---

## 📊 测试结果记录

| 测试项 | 结果 | 备注 |
|-------|------|------|
| 临时客户累计收益 | ⬜ 待测试 | |
| 登录用户功能 | ⬜ 待测试 | |
| 数据迁移 | ⬜ 待测试 | |
| 切换用户状态 | ⬜ 待测试 | |
| 边界情况 | ⬜ 待测试 | |
| 性能测试 | ⬜ 待测试 | |

---

## 🚀 发布前检查清单

- [ ] 所有测试用例通过
- [ ] 代码审查完成
- [ ] 文档更新完成
- [ ] 回滚方案准备就绪
- [ ] 监控指标配置完成
- [ ] 用户通知准备就绪
- [ ] 灰度发布计划确认
